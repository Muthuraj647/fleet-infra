apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: grafana-agent
  annotations:
      prometheus.io/scrape: 'false'
      prometheus.io/port:   '3000'
spec:
  selector: 
    app: grafana
  type: NodePort  
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 32000
